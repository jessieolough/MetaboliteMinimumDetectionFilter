# MetaboliteMinimumDetectionFilter
Often, metabolomics data can contain many spuriously-detected metabolites that can make a dataset unnecessarily messy. This repository describes the steps on how to use two Python scripts to filter out these metabolites, leaving you with a tidier dataset to perform statistical analyses on.
